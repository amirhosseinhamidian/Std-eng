import styles from "./Subscription.module.css";

const Subscription = () => {
  return (
    <div className={styles.subscription}>
      <b className={styles.subscription1}>Subscription</b>
    </div>
  );
};

export default Subscription;

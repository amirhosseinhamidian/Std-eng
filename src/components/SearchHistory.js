import styles from "./SearchHistory.module.css";

const SearchHistory = () => {
  return (
    <div className={styles.searchHistory}>
      <b className={styles.searchHistory1}>Search History</b>
    </div>
  );
};

export default SearchHistory;
